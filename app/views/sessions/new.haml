= form_with url: authenticate_path, data: { remote: true, turbo: false } do |f|
  %p= f.text_field :email, placeholder: 'Email'
  %p= f.password_field :password, placeholder: 'Password'
  %p= f.submit 'Login'

%p= flash[:alert]

= turbo_frame_tag dom_id(section) do
  = form_with model: section, class: 'd-flex align-items-center gap-2' do |f|
    = f.text_field :name, class: 'form-control form-control-sm'
    .form-actions.d-flex.gap-2
      = f.submit 'Save', class: 'btn btn-sm btn-primary'
      = link_to 'Cancel', section_path(section), class: 'btn btn-sm btn-secondary'

%p= form.text_field :name, placeholder: 'Name'
%p= form.text_field :link, placeholder: 'Link'
%p= form.collection_select :section_id, Current.cookbook.sections.by_position, :id, :name, include_blank: true
%p= form.rich_text_area :instructions, placeholder: 'Notes'

= form_with model: [recipe, Cook.new(date: Time.zone.today)], class: 'cook-form' do |f|
  .form-group
    = f.label :date, class: 'form-label'
    = f.date_field :date, value: Time.zone.today, class: 'form-control'
  = f.submit 'Log Cook', class: 'btn btn-primary'
